<h1>All Tags</h1>

<ul>
  <% @tags.each do |tag| %>
  <li>
    <%= link_to tag.name, tag_path(tag)%>
    <%= link_to "[X]", tag_path(tag), method: :delete, data: {confirm: "Really delete tag \"#{tag.name}?\""} %>
  </li>
  <% end %>
</ul>
<br />

<%= link_to "<< Back to article list", articles_path %>